{
  "name": "notion-highlighter-extension",
  "version": "2.0.0",
  "description": "Advanced highlighting extension for Notion with floating overlay system",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "dev": "npm run clean && npm run watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "private": true
}